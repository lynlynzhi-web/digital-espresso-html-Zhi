All HTML files and images are organized within a single project folder. The pages are interconnected through a consistent global navigation menu on each page. The site is structured using HTML elements like header, nav, main, section, aside, and footer to create a clear document outline.
